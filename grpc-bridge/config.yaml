name: gRPC Bridge
description: Use gRPC calls to control and react to Home Assistant events, entities
  and more
version: 0d4020a
url: https://github.com/moonen-home-automation/grpc-bridge
slug: grpc-bridge
init: false
arch:
- aarch64
- amd64
- armv7
homeassistant_api: true
ports:
  9091/tcp: 9091
ports_description:
  9091/tcp: gRPC
image: ghcr.io/moonen-home-automation/grpc-bridge/{arch}
